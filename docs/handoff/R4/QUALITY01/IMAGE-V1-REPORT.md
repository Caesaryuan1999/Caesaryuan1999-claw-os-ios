# Image v1 — fixed source and prepared native coverage

The isolated branch is `codex/r4-quality01-ios-image`, based on `4e2ba386d5b07adfa09806e75bc8914a9fb85604` (production `db514`). CI48 continues to refer exclusively to integration `00425c0`; this image branch has not been pushed or run on macOS.

Production commits: `5c973463dbd945f5b8951d4ad700f364b3603848` (geometry), `93ea7ee765fa3694dcd4dce3bfcc98ba69c821f8` (states/owned retry), `446bc538fdbab9d7a2c992e356c4895a73137c25` (new bound preview consumption and unavailable copy). The cumulative production boundary remains the seven paths in IMAGE-V1-PLAN. The successor closes a source-level asymmetry found by independent review: a new ready glyph URL carried its binding but the old preview consumer previously ignored it. Only URLs carrying a binding get the new check; the old unbound route remains compatible.

Full IM only opts in. Reliable metadata wins, otherwise decoded inline dimensions win; ordinary non-long images return to the complete original geometry/path. Long H/W >= 3 uses TOP 3:4 up to 240 x 320. Only a source with neither reliable dimensions nor decoded inline data receives the fixed unknown 3:2 canvas up to 240 x 160. Success FITs the pixels within that canvas. No received metadata is changed. Quote/video/upload paths do not opt in. Nonfinite/nonpositive available bounds fail closed before allocating a canvas.

The real attachment retains its original owned context and URL. Retry additionally rechecks the page, message/entity value, cell binding, owner and attempt. Window detach/reuse cancels the binding; relayout alone does not restart a request. Loading/failed/ready overlays retain the canvas. `unavailable` says “图片当前不可用”; failed says “图片未加载”. Retry is transparent teal text with a nominal 112 x 48 minimum target where the canvas permits it. The system font is retained. If the full title cannot fit vertically, the visible action shortens to “重试” and accessibility remains “重新加载”; an extremely small canvas can scroll its content rather than silently shrink typography or grow the message. Ready ordinary taps use the existing preview, and bulk selection retains precedence.

Six new methods are prepared in `TinodiosUITests/ImageMessageLayoutTests.swift`, using the existing App-hosted `TinodiosVoiceLayoutTests` target:

1. Actual FullFormatter/FormatNode preserves ordinary geometry; metadata/decoded/unknown precedence, quote/video opt-out and invalid/narrow bounds.
2. Actual UIKit renderer samples red/blue source pixels to distinguish top cropping from unknown FIT margins.
3. Actual MessageViewController/MessageViewLayout/MessageCell with synthetic incoming/outgoing messages, light/dark and standard/AX fonts; original PNG and geometry attachments.
4. Actual AsyncImageTextAttachment/owned loader with controlled transport delivery: same canvas, explicit retry, cancel/rebind and retired owner.
5. Actual controller retry/preview consumers: message/entity/page/binding gates, bulk rejection, true window detach/rebind without automatic request, reuse and delayed completion. The existing segue handoff is observed, not a real image-preview navigation.
6. Actual loopback TCP, URLSession/Kingfisher and the owned attachment: HTTP 403 then explicit 200 retry, synthetic same-origin auth header presence, original cell button hit geometry, AX label, transparent action and fixed successful FIT canvas.

The isolated SDK uses a real SQLite store and a controlled current-slot closure, not a logged-in App account. Controlled transport tests are distinct from the real HTTP method. Button `sendActions` and UIKit `hitTest` are not device touches. Existing 330 native methods and 3 navigation methods are unchanged; the new expected count is 336 + 3, **all Image v1 Swift/renderer/network methods NOT_RUN** until an exact reviewed macOS candidate executes. The child deliberately does not yet inherit the independent integration AAC test successor.

Windows `IMAGE-V1-STATIC.json`: all 44 existing source policies passed unchanged. `git diff --check` passed. PBX adds one file reference/build-file/group/source membership, the existing CI selector adds only `ImageMessageLayoutTests`; no new target, dependency, Pod, workflow, timeout, SDK, DB or protocol change. These checks do not establish Swift compilation, UIKit layout, network or screenshot success.

Recovery: retain integration `00425c0`, the original image base and every CI44–48 failure. After source review, cherry-pick the three production commits in the order above, then the dedicated tests/wiring commit, onto the intended integration candidate. Do not merge the child wholesale or repeat the common voice ancestors. To undo only this unit, revert its test commit and the three production commits in reverse order on a clean dedicated branch; no account/store migration or private configuration change is involved. Do not reset/clean the integration worktree or remove original artifacts. No push or CI was performed for Image v1 in this report.
