# CI46 help explanation height correction

Separate from the AAC test-only scheduling successor. Exact red source: `aaeab1b1b8527606107393aeb11d795072e76718`, run `35470676087`, artifact `10592579074` (original hash and paths in `CI46-DIAGNOSIS.md`).

The failure is the actual help contact explanation, not the account header. `CoreListLayoutTests.swift:555` invokes unchanged `readable(label)`; original final geometry shows width 232 / height 400 / font 45. Required height at that real width is 429.667 points. Original `5B18AB10-A946-4876-A002-6FD7E21C2CAC.png` visibly omits the ending “言。” below “暂不接收留”. Teardown already laid out and drew the current page. The production estimate used table width minus 40 (280 points), so merely waiting longer or reducing text would not address the width mismatch.

Only `Tinodios/SettingsHelpViewController.swift` changes production behavior. The row-height delegate now measures the real laid-out `textLabel.bounds.width`. The old estimate remains only for a label without a positive actual width. `viewDidLayoutSubviews` watches both table width and each of the three actual explanation-label widths; a changed width performs a guarded table height update, including when table width itself did not change. A newly displayed static row requests layout without starting a nested update from `willDisplay`. Caches are assigned before the height update and a reentrancy flag prevents nested table updates. There is no timer, sleep, forced font reduction, fixed 232/88-point magic inset, async network operation or new product behavior.

`CoreListLayoutTests.swift` keeps all six original test bodies and the original closed-help text/state/license/readability/containment assertions. The existing real help helper additionally checks the same 320-point page across original AX → Large → AX → Large and records same-frame actual label width/height, expected text height, production delegate row height and actual row height. The three-second bound remains. Standard help/license coverage remains intact. Cumulative native method count is unchanged at 327.

Windows checks: exact six old method bodies matched after LF normalization; 13 existing helper assertion lines retained; all production localized strings unchanged; `git diff --check` passed; `Scripts/ci/test_premium_secondary_ui_policy.py` passed. These are source checks, not a UIKit result. The new fix and screenshots remain **NOT_RUN** pending one reviewed Mac candidate. No production networking, authentication, database, message state, Pods, target, workflow or selector changes.

Rollback: revert this standalone two-code-file commit. The CI46 red ZIP and PNG/JSON remain unchanged. The compact voice worktree remains a separate uncommitted WIP and is not included.
