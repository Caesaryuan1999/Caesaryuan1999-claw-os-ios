# CI45: known-run fixture completion compilation

Source `25608fd260ae08a83d60bda192571367b7836ed6`, run `35469991480`, job `105969075600`, attempt 1. The locked MobileVLCKit installation and SDK 44 methods passed. Storage/App-hosted tests failed during compilation; zero storage methods executed. The six new ordinary-AU methods and previous eleven VoiceLayout methods were NOT_RUN. Navigation, unsigned packaging and cold launch were skipped. There are no new runtime screenshots for this candidate.

Original artifact `10592068258`: 814,708 bytes, SHA-256 `9be4f57715600f197d07bc1c35cf8b9792d3b2056efc3630ccdce3c6b2fe94d9`. Local original: `evidence/CI45-evidence.zip`; receipt: `evidence/CI45-evidence.download.json`. The complete original `storage.log` contains 17,107 lines and exactly one compiler `error:` record: `AssistantKnownRunTests.swift:100:67`, invalid conversion from a throwing function to a non-throwing completion. Its SHA-256 is in `CI45-FIX-CHECKS.json`. This full-log scan does not establish that later compilation units are error-free.

The existing capability-rejection fixture placed `XCTAssertThrowsError(try result.get())` inside the service's non-throwing completion. The actual Mac importer inferred this closure as throwing. The narrow successor keeps the callback non-throwing by capturing its Result, then checks the original synchronous-called and zero-new-request assertions, explicitly unwraps the captured result, and runs the original ThrowsError assertion in the existing throwing test scope.

Only `TinodiosUITests/AssistantKnownRunTests.swift` changes. All thirteen method names and existing assertions remain; the added unwrap prevents a missing callback/result from satisfying an expected-error assertion. Inverting this small block reproduces the parent's entire test file exactly. Production, the six AU methods, Pods, PBX, selectors and workflow are unchanged. No timeout, input, response or acceptance criterion is relaxed.

Windows checks: exact inverse, thirteen method identities, complete compiler-error scan and `git diff --check` passed. Successor Swift execution remains NOT_RUN until the independently reviewed fixed SHA runs on Mac. Expected cumulative count remains 327 native plus 3 navigation, not a passed count. To roll back this unit, restore this one test file from `25608fd`; retain CI44/CI45 original evidence and reviewed production.
