# CI48 — both isolated AAC controls lack a recorded finish

Exact candidate `00425c0ed7a322f5e461648a58471453a6e59c1c`, run `35475312263`, job `105983445715`, attempt 1. The sole authorized non-force push/dispatch took place at 2026-09-20 07:09 +08. The job naturally failed; it was not cancelled or retried. Its 45-minute job limit, the original two 3-second AAC waits, all original assertions and selectors were unchanged.

Original artifact `10593109482`, 105348502 bytes, SHA-256 `bd8aa5a1b119ebf83dab5347789c4374320d59a11f17274c6e8f6941174fbe35`, is preserved at `evidence/CI48-evidence.zip`. Extraction: `evidence/CI48-original/ios-01-a-20260919-230946`. SDK summary: 44 passed. Storage summary: 285 passed, 1 failed, 0 skipped. Total: **329 passed / 1 failed / 0 skipped**. Core6 and compact-voice3 passed; Voice20 was 19/1 (old11 passed, owned-AU6 was 5/1, compact3 passed). Navigation, unsigned packaging and cold launch did not run.

The failing method is `VoiceLayoutTests.testOwnedAudioActualAACPlaybackPauseResumeAndFiniteSeek`, line 734, `productionResult == .completed`. The bare result assertion is later at 735, not earlier; this lone failure line cannot establish bare success. The original at-deadline attachments establish the actual comparison:

- Bare: `storage-attachments/A3F732C2-F4D5-435E-AD5D-5178E784312A.json`; `bare_wait_XCTWaiterResult(rawValue: 2)` at 3.135201 seconds, successful same-engine finishes 0.
- Production: `storage-attachments/8C78CC62-3C45-4645-95B8-9E47B328CCBA.json`; `production_wait_XCTWaiterResult(rawValue: 2)` at 3.107625 seconds, successful same-engine finishes 0.

Both accepted playback; both report duration 1, loops 0, the installed observer and the same engine. Both clocks reach 1 around 1.25 seconds while `isPlaying` remains true through the deadline. Main-thread samples continue. Both use Playback/Default, 48000 Hz and a two-channel Speaker output. Production scope remains current. No finish or decode-error event was recorded. This does not support the prior main-queue starvation hypothesis or a production-only state-transition cause. The initial player had been retired before the bare control, and the bare player stopped before production. The original AAC bytes remain unchanged; root's successful FFmpeg decode is a separate decoder observation, not proof of AVAudioPlayer completion.

A shared observer blind spot remains: both delegate implementations originally invoked AV getters before writing their first event or forwarding the callback. Absence of a recorded event therefore does not prove absence of delegate entry. The independent successor changes **only `OrdinaryAudioFinishObserver` in the test file**: record real entry time/thread/object identity/success before any AV getter; forward the unchanged real callback; queue detailed readings asynchronously. All AV/session getters stay outside the observation lock. No callback is synthesized, and no clock value is accepted as completion. All 20 test methods, 3-second waits, same-engine finish count and production-ended assertions remain byte-identical.

The successor removes an evidence blind spot; it does not establish that a getter blocked, that the fixture/platform is at fault, or that playback is fixed. Windows diff/region checks passed (`evidence/CI48-observer-checks.json`); successor Swift execution remains **NOT_RUN**. No new CI was dispatched. Image v1 remains a separate child and separate commits. Preserve CI44–48 originals and independent source/renderer evidence; do not replace these failures with later screenshots or source-only conclusions.
