# CI18 dependency evidence failure and minimal repair

Original candidate: `9d3a4b4b0d1c369e11b0205b27ef5255e8737a6e`. Parent-reported Mac run `35329520667`, job `105550370362`: SDK 41 passed, then the embedded dependency evidence Python failed opening `Pods/Local Podspecs/MobileVLCKit.podspec.json`. Storage/VLC/navigation did not start; attachment exports were NOT_RUN. Artifact `10540388268`, 172057 bytes, parent-provided SHA-256 `bfdb4a42c5b77cf750684f891fca9ae6ccc2d274f2c6b8abfe0f340d8a6dec28`. The original failure is retained; it is not a VLC compilation or playback failure.

Root cause: the script incorrectly assumed a regular spec-repository pod retained a local podspec. CocoaPods 1.15.2's actual install method removes that file for ordinary, non-predownloaded/non-local/non-external pods. The fixed official source was read, not inferred from the sandbox path helper: [PodSourceInstaller.install!](https://raw.githubusercontent.com/CocoaPods/CocoaPods/1.15.2/lib/cocoapods/installer/pod_source_installer.rb), lines 61–65.

Only production-independent `Scripts/ci/verify_publish_outcomes_macos.sh` changes. It reads the installed `Pods/Manifest.lock` and repository `Podfile.lock` PODS / SPEC CHECKSUMS sections, requiring one exact MobileVLCKit entry in each, version `3.6.0`, and matching frozen spec checksum `8fe98ae53b7464f32e4bdf527cc7d53053e4d3a5`. Both lock file SHA-256 values are recorded. The narrow section reader rejects absent/duplicate/unknown-format evidence; it does not search other YAML sections for a convenient match.

The existing actual xcframework/simulator-slice path containment, three header API presence/hash checks, framework metadata and installed binary SHA-256 remain unchanged. Missing framework/header/binary still fails. The previous archive SHA came from the official podspec declaration, not a re-hash of the archive downloaded in this runner. The new output therefore removes `archiveSHA256` and explicitly reports archive verification NOT_PERFORMED. Spec checksum is identified as a spec checksum, not archive or binary integrity proof. No `pod spec which/ipc`, cache-path guess, dependency update, remote fetch or latest-version fallback was introduced.

Windows regression executed the actual old/new embedded Python blocks against synthetic installed trees and the actual repository lockfile. The old block reproduced FileNotFoundError without Local Podspecs. Fifteen new cases passed: valid/no-local-spec and CRLF accepted; missing manifest, one/both changed versions or checksums, duplicate sections/entries, wrong section, missing header/API/binary, and escaped framework path rejected. Accepted cases verified reported lock/header/binary data and that no archive verification was claimed. These are Python adapter checks, not a CocoaPods installation or Swift/native run. Bash syntax and diff checks passed. Evidence: `ci18-lock-checks.json`.

All four VLC test methods, all other native test sources, selectors, project, Podfile/lockfile, artifact workflow, and production source are unchanged from 9d3a4b4. The prior 200 native (41 SDK + 159 storage) plus navigation 3 remain expected, not passed. Await exact CI19 to establish compilation, real VLC measurements and original attachments. A green observation test must still be reviewed for any reported redirect/retirement/cache safety gap.
